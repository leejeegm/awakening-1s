# Supabase (자깨초시 실험 데이터)
# https://supabase.com/dashboard 프로젝트 설정에서 복사하세요.
NEXT_PUBLIC_SUPABASE_URL=https://lxvuyhiflpdyeegjrsva.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 관리자 기능 (기록 삭제/수정). 서비스 역할 키는 Dashboard → Settings → API → service_role (비공개 유지).
SUPABASE_SERVICE_ROLE_KEY=
# 관리자 전용 비밀번호. /admin 접속 시 이 비밀번호로 로그인 후 삭제/수정 가능.
ADMIN_SECRET="s*****오****"

# 관리자 로그인 실패 레이트 리밋 (선택, 기본: 15분 창에 12회 실패 시 15분 잠금)
# ADMIN_LOGIN_MAX_FAILS=12
# ADMIN_LOGIN_FAIL_WINDOW_MS=900000
# ADMIN_LOGIN_LOCKOUT_MS=900000

# Gemini 호출 상한 — 닉네임·키별. 기본은 인스턴스 메모리(서버리스 베스트 에포트).
# Supabase에 마이그레이션 017 적용 후 true로 두면 DB 원자 카운터(다중 인스턴스 공유)
# GEMINI_RATE_LIMIT_USE_DB=true
# GEMINI_RATE_LIMIT_WINDOW_MS=3600000
# GEMINI_RATE_LIMIT_MAX_PER_KEY=80

# Gemini (권장): 따뜻한 한마디·인사이트 카드·주간 요약은 1차로 Gemini 호출.
# GEMINI_API_KEY=... 를 쓰거나 아래처럼 별칭 변수도 가능합니다.
# GEMINI_JAKKAE_API_KEY=...
# GEMINI_MODEL=gemini-1.5-flash

# OpenAI GPT-4o (선택·정밀): ① 1초 찰나(1s) 컨텍스트가 있는 요청만 텍스트 보강
#    ② 공개 기록 검수: Gemini 불가 시 폴백, 또는 Gemini가 warn일 때 1s 공개만 재판정
# 발급: https://platform.openai.com/api-keys → Create new secret key
# OPENAI_API_KEY=sk-proj-...

# 모더레이션 삭제(보관) 건 완전 폐기까지 최소 보관 일수 (기본 30). 7·30·90 등으로 조정 가능.
# MODERATION_QUARANTINE_DAYS=30

# 공개 저장: 룰 통과 후에도 Gemini+OpenAI 분류를 못 받으면 공개 차단(운영 보수 모드). 기본은 끔(false).
# MODERATION_STRICT_NO_AI=true

# 서버 이미지/웹툰 (승인 사용자). MVP 기본: gemini (GEMINI 키 있으면 자동)
# IMAGE_PROVIDER=gemini
# IMAGE_PROVIDER=webui
# IMAGE_PROVIDER=pollinations
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# GPU WebUI (IMAGE_PROVIDER=webui 일 때)
# IMAGE_ENGINE_URL=https://YOUR-POD.proxy.runpod.net/sdapi/v1/txt2img
# POLLINATIONS_IMAGE_BASE_URL=https://image.pollinations.ai/prompt
# 유료 보고서: 서버 생성 이미지 자동 첨부 최대 장수 (기본 3, 4컷은 2×2 한 장으로 첨부)
# PREMIUM_REPORT_AUTO_IMAGE_MAX=3
# PREMIUM_REPORT_BUCKET=premium-reports
# 클라이언트 로컬 생성 기본 주소(선택). 미설정 시 브라우저 기본값은 http://127.0.0.1:7860/sdapi/v1/txt2img
# NEXT_PUBLIC_LOCAL_IMAGE_ENGINE_URL=http://192.168.0.10:7860/sdapi/v1/txt2img
# Supabase Storage 버킷 이름 (대시보드에서 생성 권장). 기본 generated
# IMAGE_BUCKET=generated
# 닉네임별 한도 (선택). 미설정 시 기본: 일 10회, 월 50회(bun/si 플랜은 월 280)
# IMAGE_DAILY_LIMIT=10
# IMAGE_MONTHLY_LIMIT_DEFAULT=50
# IMAGE_MONTHLY_LIMIT_BUN=280
# IMAGE_MONTHLY_LIMIT_SI=280
#
# Vercel Hobby 대응 (기본값이 Hobby 친화적)
# IMAGE_SERVER_ASYNC=true          — job 생성 후 /api/ai/image/jobs/[id] 폴링 (기본 true)
# IMAGE_SERVER_TIMEOUT_MS=9000     — 엔진 fetch 타임아웃(ms)
# IMAGE_SERVER_MAX_STEPS=14
# IMAGE_SERVER_IMAGE_WIDTH=512
# IMAGE_SERVER_IMAGE_HEIGHT=512
# IMAGE_SERVER_COMIC_SIZE=512      — 4컷도 기본 512 (Pro/GPU 여유 시 768·1024로 올릴 수 있음)
# IMAGE_SERVER_POLL_INTERVAL_MS=2500
# IMAGE_SERVER_POLL_MAX_MS=120000
# POST body { "sync": true } — 한 요청 동기 생성(테스트용)

# 참고: 실명/전화번호 수집 없이 닉네임·한줄 기록만 저장합니다.
